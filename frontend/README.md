# Frontend Workspace

这里管理 DeckGo 的前端相关项目：

- `web/`：Vue 3 工作台
- `renderer/`：Node + PptxGenJS 渲染器
- `packages/deck-core/`：共享类型和预览辅助
- `packages/template-kit/`：模板目录和模板辅助方法

这个 workspace 只服务于前端和渲染器，不承担整个仓库的主入口角色。
